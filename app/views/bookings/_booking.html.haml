%ul
  %li
    %h6= link_to "#{booking.date_time.to_s(:booking_title)}", booking_path(booking)
    = link_to "Create Booking", "#"
    |
    / = link_to "#{booking.workouts}", "#"
    |
    = link_to "#{booking.client}", "#"
    |
    = link_to "Delete Booking", booking_path(booking), :method => :delete, :alert => "Are you sure you want to delete this booking?"