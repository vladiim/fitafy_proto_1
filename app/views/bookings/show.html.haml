%h2= "Booking - #{@booking.booking_date}"

%p= "At #{@booking.booking_time}"

%p With - client here

=link_to "Edit Booking", edit_booking_path(@booking)

|

= link_to "Delete Booking", booking_path(@booking), :method => :delete, :alert => "Are you sure you want to delete this booking?"