%p{:datetime => "#{@booking.wo_date}#{@booking.wo_time}"}=  "On #{@booking.booking_date} " "at #{@booking.booking_time}"

%p
  %strong Client:
  = link_to "#{@client.username.titleize}", user_path(@booking.client)

%strong= @workout.title

= render 'exercises'

/ add, remove, update exercises

%br

=link_to "Edit Booking", edit_booking_path(@booking)

|

= link_to "Delete Booking", booking_path(@booking), method: :delete, confirm: "Are you sure you want to delete this booking?"