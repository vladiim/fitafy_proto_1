= simple_form_for @booking, url: booking_request_path(@booking), html: { method: :delete } do |f|
  = f.input :status, as: :hidden, input_html: { value: "declined" }
  = f.button :submit, "Decline", :class => "btn btn-success"